# 개인 금융 리서치 AI 프로젝트 기획서

## 1. 프로젝트 개요

### 프로젝트명
**Daily Investment Intelligence — 개인 맞춤형 AI 투자 리서치 시스템**

### 한 줄 소개
매일 정해진 시간에 시장 데이터, 뉴스, 공시 및 리서치 자료를 수집하고, 관심 섹터와 종목을 정량적으로 분석한 뒤 AI Agent가 근거 기반의 Daily Investment Report를 생성하는 개인용 투자 리서치 시스템.

### 핵심 목표
단순히 LLM에게

> "오늘 어떤 주식이 좋아?"

라고 질문하는 시스템을 만드는 것이 아니다.

다음과 같은 End-to-End AI/ML 시스템을 직접 설계하고 운영하는 것을 목표로 한다.

```
Data Collection
      ↓
Data Storage
      ↓
Data Processing
      ↓
Quantitative Analysis
      ↓
Information Retrieval
      ↓
AI Agent
      ↓
Investment Research
      ↓
Report / API
      ↓
Deployment
      ↓
Monitoring
```

이를 통해 현재 보유하고 있는 **Python / ML / Data Analysis** 역량을 **Backend, Database, Network, Infrastructure, CI/CD** 영역으로 확장한다.

---

## 2. 문제 정의

현재 금융 정보는 여러 곳에 분산되어 있다.

- 주가 / 거래량
- 뉴스
- 공시
- 기업 실적
- 애널리스트 리포트
- 거시경제 지표
- 산업 정보

투자자는 매일 이를 직접 확인하고 다음 질문에 답해야 한다.

1. 오늘 시장에서 무엇이 변했는가?
2. 내가 관심 있는 섹터 중 어디가 강한가?
3. 어떤 종목이 상대적으로 주목할 만한가?
4. 그 이유는 무엇인가?
5. 이 판단을 뒷받침하는 데이터와 뉴스는 무엇인가?

따라서 본 프로젝트에서는 **정보 수집 → 분석 → 근거 검색 → 해석 → 리포트 생성** 과정을 자동화한다.

---

## 3. 기술 스택 학습 로그 운영 방식

### 3.1 목적

이 프로젝트는 단순히 "동작하는 시스템"을 만드는 것이 목표가 아니라, **AI/ML 엔지니어가 Backend / DB / Infra / Network / CI-CD 영역의 CS 지식을 실전으로 습득**하는 것이 목표다.

따라서 새로운 기술 스택을 도입할 때마다, 사용하고 끝내는 것이 아니라 **"왜 이걸 골랐고, 무엇을 배웠는가"를 블로그 리뷰 형태의 문서로 남긴다.** 이 문서는 나중에 실제 블로그 포스팅이나 포트폴리오 자료로도 재사용할 수 있어야 한다.

### 3.2 운영 규칙

- 새로운 기술 스택(라이브러리, DB, 인프라 도구, 프로토콜 등)을 프로젝트에 **처음 도입하는 시점**에 학습 로그를 작성한다.
- 문서는 `docs/tech-notes/` 디렉토리 아래에 **기술 하나당 파일 하나**로 작성한다.
  - 파일명 규칙: `docs/tech-notes/{단계}-{기술명}.md`
  - 예: `docs/tech-notes/01-data-collection-yfinance.md`, `docs/tech-notes/03-storage-timescaledb.md`, `docs/tech-notes/07-agent-langgraph.md`
- **구현 코드를 작성하기 전에** 해당 기술에 대한 학습 로그를 먼저 작성(혹은 최소 초안 작성)하고, 구현이 끝난 뒤 "실제로 써보니 어땠는지" 섹션을 채워 넣는 방식으로 **초안 → 구현 → 회고**의 흐름을 따른다.
- 같은 기술이라도 프로젝트 진행 중 이해도가 깊어지면 문서를 갱신한다. 수정 이력을 남기고 싶다면 문서 하단에 "업데이트 로그"를 추가한다.
- Claude Code로 구현을 진행할 때, 새로운 기술 스택이 코드에 등장하면 **구현과 함께 반드시 해당 학습 로그 파일도 같이 생성/갱신**한다. 즉, "기능 구현 PR"과 "학습 로그"는 항상 세트로 취급한다.

### 3.3 학습 로그 템플릿

아래 템플릿을 그대로 각 기술 노트 파일에 사용한다.

```markdown
# [기술명] 정리

- 카테고리: (Data Collection / Storage / Processing / Quant Analysis / Retrieval / AI Agent / API / Deployment / CI-CD / Monitoring 중 택1)
- 도입 시점: YYYY-MM-DD
- 관련 코드 위치: (파일/모듈 경로)

## 1. 이 기술을 이해하기 위한 사전 지식
- 이 기술을 제대로 이해하려면 미리 알아야 하는 배경 지식/용어를 나열
- (예: Redis를 다루기 전에 알아야 할 것 → 메모리 vs 디스크 저장, key-value 모델, 캐시 무효화 개념 등)

## 2. 기술 개념 정리
- 이 기술이 정확히 무엇이고, 내부적으로 어떻게 동작하는지
- 비슷한 대안 기술들과 비교했을 때 어떤 카테고리에 속하는지
- 핵심 동작 원리를 그림/의사코드 없이 말로 설명할 수 있을 정도로 정리

## 3. AI/ML 엔지니어 관점에서 알아두면 좋은 개념
- ML/AI 엔지니어링 배경에서는 낯설 수 있는 CS 개념 중, 이 기술을 계기로 짚고 넘어가야 할 것
- (예: 메시지 큐를 다루며 → at-least-once vs exactly-once delivery, backpressure 개념 등)

## 4. 장점 (이 기술이 잘하는 것)
- 실제로 이 프로젝트에서 사용하며 체감한 장점 위주로 작성 (일반적인 마케팅 문구 나열 X)

## 5. 한계점 / 트레이드오프
- 이 기술의 약점, 이 프로젝트 규모에서는 과할 수도 있는 부분, 실제로 부딪힌 문제
- 대안이 있었다면 무엇을 포기하고 이걸 선택했는지

## 6. 왜 이 프로젝트에서 이 기술을 선택했는가
- 프로젝트의 제약 조건(개인 프로젝트, 데이터 규모, 예산, 학습 목적 등)을 고려했을 때
  이 기술이 합리적인 선택이었던 이유
- 만약 실서비스/대규모 트래픽이었다면 다른 선택을 했을지에 대한 코멘트

## 7. 참고 자료
- 공식 문서, 참고한 글, 관련 개념 설명 링크 등
```

### 3.4 예상 기술 스택 카테고리 (초안)

아래는 파이프라인 단계별로 학습 로그가 필요할 것으로 예상되는 영역이다. 실제 기술 선택은 구현 단계에서 확정되며, 확정되는 대로 아래 표를 갱신하고 `docs/tech-notes/`에 대응하는 문서를 만든다.

| 파이프라인 단계 | 다뤄볼 만한 기술/개념 후보 | CS 지식 확장 포인트 |
|---|---|---|
| Data Collection | REST API 연동, 웹 스크래핑, 스케줄링(cron/Airflow) | 네트워크 요청/응답, rate limiting, 인증(OAuth/API Key) |
| Data Storage | SQLite → PostgreSQL, 시계열 DB(TimescaleDB/InfluxDB) | 관계형 DB 정규화, 인덱스, 시계열 데이터 모델링 |
| Data Processing | pandas/Polars 배치 처리, 메시지 큐(선택) | 배치 vs 스트리밍, ETL/ELT 개념 |
| Quantitative Analysis | 팩터 기반 스코어링, 기술적 지표 라이브러리 | 통계적 검증, 벡터화 연산 |
| Information Retrieval | 벡터 DB(예: pgvector, Chroma), BM25/하이브리드 검색 | 임베딩, 인덱싱 구조(HNSW 등), 검색 랭킹 |
| AI Agent | LangGraph/직접 구현 Agent 루프, 프롬프트 오케스트레이션 | 상태 관리, tool calling, 컨텍스트 관리 |
| Report / API | FastAPI, 템플릿 렌더링 | REST API 설계, 인증/인가, 비동기 처리(async/await) |
| Deployment | Docker, (선택) 클라우드 VM/서버리스 | 컨테이너화, 프로세스 격리, 환경변수 관리 |
| CI/CD | GitHub Actions | 빌드/테스트/배포 파이프라인, 자동화 트리거 |
| Monitoring | 로깅, 알림(Slack/Email), (선택) Grafana | 관측 가능성(observability), 알람 임계치 설계 |

### 3.5 Claude Code 작업 흐름 예시

새로운 기술 스택을 도입하는 작업 단위는 다음 순서로 진행한다.

1. **작업 정의**: 이번에 구현할 기능과 여기에 필요한 신규 기술 스택을 명시
2. **학습 로그 초안 작성**: `docs/tech-notes/` 에 템플릿 기반으로 1~3번 섹션(사전 지식, 개념, AI/ML 엔지니어 관점) 먼저 작성
3. **구현**: 실제 코드 작성 및 동작 확인
4. **회고 작성**: 구현 중 겪은 이슈를 반영해 4~7번 섹션(장점, 한계점, 선택 이유, 참고 자료) 작성
5. **커밋**: 코드와 학습 로그를 함께 커밋 (커밋 메시지에 `docs(tech-notes): ...` 형태로 학습 로그 갱신 사실을 명시)

---

## 4. 확정된 프로젝트 결정

구현 단계에서 계속 되물어야 하는 큰 선택들을 여기에 못 박아 둔다.
결정을 바꿀 때는 항목을 지우지 말고 **"변경 이력"에 사유와 함께 추가**한다.

| # | 항목 | 결정 | 사유 |
|---|---|---|---|
| D-1 | 대상 시장 | **미국 시장** | 데이터 소스(yfinance, SEC EDGAR)의 API 문서화와 데이터 품질이 가장 좋아, 수집 자체와 씨름하는 대신 파이프라인 설계에 집중할 수 있다. 임베딩·LLM 처리에서도 영문이 유리하다. |
| D-2 | 기술 선택 기준 | **학습 우선 — 단계적 업그레이드** | 처음엔 가장 단순한 선택(SQLite, 로컬 실행)으로 동작시킨 뒤, 한계를 직접 겪고 나서 업그레이드한다. 학습 로그의 "5. 한계점"과 "6. 선택 이유" 절이 실제 경험으로 채워지는 것이 이 프로젝트의 산출물이기 때문이다. |
| D-3 | 운영 환경 | **로컬에서 시작 → 안정화 후 클라우드** | 초기 비용 0, 의사결정 미룰 수 있음. 단 M5에서 클라우드 이전을 **반드시 수행**한다. "내 PC가 꺼지면 멈춘다"를 벗어나야 배포·모니터링 학습이 성립하기 때문이다. |

### 4.1 D-2의 함의 — 의도된 재작업

D-2는 **재작업을 비용이 아니라 커리큘럼으로 취급하겠다는 선언**이다. 특히 다음 두 지점은 처음부터
"제대로" 만들지 않고, 일부러 단순하게 시작했다가 갈아탄다.

- **SQLite → PostgreSQL** (M1 → M3): 동시 접근, 타입 시스템, 확장(pgvector)의 필요를 직접 겪은 뒤 이전한다
- **로컬 스케줄러 → Docker + 클라우드** (M1 → M5): 환경 재현성과 가용성의 필요를 겪은 뒤 이전한다

따라서 M1~M2의 코드는 **버려질 것을 전제로 짜지 않되, 갈아타기 쉽게 경계를 긋는다**.
구체적으로 저장소 접근은 처음부터 리포지토리 계층 뒤에 숨기고, 분석 로직이 SQL 방언에 직접
의존하지 않게 한다.

---

## 5. 구현 로드맵

각 마일스톤은 **"무엇이 되면 끝인가"(완료 조건)** 로 정의한다. 기능 목록이 아니라 관측 가능한
상태로 적어야, 다음 단계로 넘어갈 시점을 스스로 판단할 수 있다.

### 5.0 분석 유니버스 (초기값)

유니버스는 **`config/universe.toml`** 에 둔다. 대상을 바꿀 때 코드를 건드리지 않기 위함이다.

- **시장 벤치마크**: `SPY`
- **섹터**: GICS 11개 섹터 SPDR ETF — `XLK` `XLC` `XLY` `XLP` `XLV` `XLF` `XLI` `XLE` `XLB` `XLRE` `XLU`
- **개별 종목**: **섹터당 4종목 × 11섹터 = 44종목** (초안 작성 완료)
- 총 수집 대상: 56 심볼 (벤치마크 1 + 섹터 ETF 11 + 개별 종목 44)

선정 기준은 두 가지다.

- **섹터당 동일 개수.** 시가총액 가중으로 뽑으면 정보기술 섹터가 절반을 차지해,
  "어느 섹터가 강한가"를 비교하는 횡단면 분석이 성립하지 않는다
- **섹터 안에서는 사업 모델을 갈라 놓는다.** 같은 업종만 넣으면 섹터 내 랭킹이 늘 비슷하게 나와
  종목 선택 신호가 무의미해진다. 예) 소재 섹터에 구리(FCX)와 금(NEM)을 같이 둬서 분산을 만든다

처음부터 S&P 500 전체를 넣지 않는다. 종목 수는 파이프라인이 안정된 뒤 늘리는 편이,
수집 실패·결측·rate limit 문제를 작은 규모에서 먼저 만나게 해 준다.

> **미검증 항목**: 티커 심볼이 데이터 소스에서 실제로 조회되는지는 아직 확인하지 않았다.
> M1 의 첫 작업으로 56개 심볼 전체의 조회 가능 여부와 과거 데이터 길이를 확인하고,
> 문제가 있는 종목은 이 파일에서 교체한다.

---

### M0 — 기반 세팅 ✅

> **완료 조건**: 새 PC에서 저장소를 clone하고 문서에 적힌 명령 몇 줄로 개발 환경이 똑같이 재현된다.

- [x] 저장소 뼈대: README, `.gitignore`, `.gitattributes`
- [x] 학습 로그 규약 및 템플릿 (`docs/tech-notes/`)
- [x] 의존성 관리 도구 선정 및 프로젝트 초기화 — **uv**, `src` 레이아웃, `uv.lock` 커밋
- [x] 설정 관리 — **pydantic-settings**, `DII_` 접두사 환경변수 + `.env`, `.env.example` 커밋
- [x] 로깅 기본 설정 — 진입점 1회 설정, Windows 콘솔 UTF-8 고정
- [x] 패키지 레이아웃 및 계층 경계 정의 → `docs/architecture.md`
- [x] 품질 도구 — ruff(린트·포맷), mypy(strict), pytest

**도입한 기술**: uv, pydantic-settings, ruff, mypy, pytest
**학습 로그**: `01-foundation-uv.md`, `02-foundation-pydantic-settings.md`
**CS 학습 포인트**: 가상환경과 의존성 잠금(lock)의 의미, 환경변수를 통한 설정 주입,
12-factor app의 config 원칙, 경계에서의 검증(fail fast), `src` 레이아웃과 import 경로

> **완료.** `uv sync` → `uv run dii config` 로 재현을 확인했다.

---

### M1 — 데이터가 매일 쌓인다 (Data Collection + Storage v1) ✅

> **완료 조건**: 수집 명령을 실행하면 유니버스 전체의 일봉 데이터가 SQLite에 저장되고,
> **같은 명령을 두 번 실행해도 중복 행이 생기지 않으며**, 중간에 실패해도 다시 실행하면 이어서 채운다.
>
> **충족 확인 (2026-08-27)**: 56/56 심볼, 70,125행 적재(14초).
> 재실행 시 336행 UPSERT 하지만 총량 70,125행 유지. 4종목을 지우고 재실행하니 정확히 복구.

- [x] 유니버스 설정 로딩 (5.0) — `config/universe.toml`, 56심볼 실조회 검증 완료
- [x] yfinance로 OHLCV 수집 — 원본 OHLC 와 수정 종가를 **함께** 저장 (`auto_adjust=False`)
- [x] SQLite 스키마 설계 — `security` / `daily_price` 분리, 복합 PK `(symbol, trade_date)`,
      횡단면 조회용 `trade_date` 인덱스, CHECK 제약, `PRAGMA user_version` 마이그레이션
- [x] **증분 수집** — 신규 종목과 기존 종목을 나눠 요청. 기존 종목은 겹침 구간만
- [x] **멱등성** — `ON CONFLICT DO UPDATE`. 3회 실행 후에도 총 70,125행 고정 확인
- [x] 실패·결측 처리 — 부분 실패해도 나머지는 저장, 종료 코드로 구분 (0/2/1)
- [ ] 로컬 스케줄 실행 — **M5 로 미룸.** D-3 이 "안정화 후 클라우드"이므로,
      스케줄링은 배포 환경이 정해진 뒤 그 환경에 맞게 붙이는 편이 낫다고 판단

**도입한 기술**: yfinance, SQLite
**학습 로그**: `03-data-collection-yfinance.md`, `04-storage-sqlite.md`
**CS 학습 포인트**: HTTP 요청/응답과 재시도, rate limiting, 관계형 스키마 설계와 정규화,
인덱스가 조회 성능에 미치는 영향, **멱등성(idempotency)** 과 UPSERT, 배치 작업의 부분 실패 복구

---

### M2 — 오늘 뭐가 강했는지 숫자로 나온다 (Processing + Quant) ✅

> **완료 조건**: 날짜 하나를 넘기면 그날 기준 **섹터 랭킹**과 **종목 스코어 표**가 나온다.
> 과거 임의의 날짜를 넣어도 그 시점에 알 수 있었던 정보만 써서 같은 결과가 재현된다.
>
> **충족 확인 (2026-08-27)**: `dii score --as-of 2026-03-17` 로 11섹터 랭킹 + 44종목 스코어 출력.
> 룩어헤드 없음을 실증 — 전체 DB 에 as-of 를 지정한 결과와, 그 이후 6,272행을 **물리적으로 지운**
> DB 의 결과가 완전히 일치했다. 같은 검증을 테스트로도 고정했다.

- [x] 배치 처리 파이프라인 — 저장 형식(long) → 분석 형식(wide) 변환.
      **파생 지표는 저장하지 않고 매번 계산한다** (7만 행이라 1초 미만, 정의가 아직 안 굳었다)
- [x] 파생 지표: 기간별 수익률, 실현 변동성(연율화), 거래량 서지(로그 비율), 섹터 대비 상대강도
- [x] 팩터 스코어링 — 4팩터(모멘텀 0.40 / 섹터 상대강도 0.25 / 저변동성 0.15 / 거래량 서지 0.20),
      횡단면 z-score → ±3 클리핑 → 가중 합산. **가중치는 미리 고정** (다중 검정 회피)
- [x] 섹터 랭킹 — 벤치마크 초과 수익률 기준. 섹터는 팩터를 합성하지 않는다
- [x] **룩어헤드 차단** — `load_frames()` 한 곳에서 `WHERE trade_date <= as_of` 로 잘라낸다
- [x] 값이 없는 팩터는 0 으로 채우지 않고 가중치에서 제외. 근거가 절반 미만이면 점수 보류

**도입한 기술**: pandas 배치 파이프라인, 팩터 스코어링
**학습 로그**: `05-quant-factor-pipeline.md` (Processing + Quant 통합)
**CS 학습 포인트**: 배치 vs 스트리밍, ETL과 ELT의 차이, 벡터화 연산과 반복문의 성능 차이,
시계열 as-of 조회, 재현 가능한 계산(결정론적 파이프라인)

> 이 단계는 기존 ML/데이터 분석 역량과 가장 가까운 구간이다. 새로 배우는 것이 적다면
> 학습 로그는 "배치 파이프라인 설계" 한 편으로 줄이고 빠르게 통과해도 된다.

---

### M3 — 왜 그랬는지 근거를 찾을 수 있다 (텍스트 수집 + Retrieval)

> **완료 조건**: "오늘 XLK가 강했던 이유"같은 질의에 대해 **관련 뉴스·공시 top-k가 근거로 회수**되고,
> 각 결과에 원문 링크와 공시 일자가 붙는다.

이 프로젝트에서 가장 무거운 마일스톤이고, **저장소 전환이 일어나는 지점**이다.

**(a) 텍스트 수집 — 완료 (2026-08-27)**

- [x] SEC EDGAR 수집 — 제출 이력 API. User-Agent 신원 표기와 초당 10회 제한 준수.
      **Form 4(내부자 거래)는 수집 단계에서 제외** — AAPL 최근 1,001건 중 588건이라 잡음이 압도적이다.
      44종목 180일치에서 411건 적재 (8-K 316 / 10-Q 83 / 10-K 6 / 정정 6)
- [x] 뉴스 수집 — **yfinance 내장 기능** 확정. 무료·키 불필요·이미 의존성.
      56종목 560건 적재. 한계는 최근 10건씩만 준다는 것 — 매일 돌려 쌓는 것으로 메운다
- [x] 중복 제거 — `(source, external_id)` 유일 제약. 재실행해도 늘지 않는다
- [x] 종목 매핑 — **다대다**. `document` + `document_symbol` 연결 테이블.
      560건 처리에 저장은 481건이었고, 차이는 여러 종목에 걸린 기사다 (한 시황 기사가 8종목)
- [x] 시각 규약 — UTC ISO 8601 통일. 공시는 날짜만 주므로 그날 자정(UTC)으로 본다

**(b) PostgreSQL 이전 / (c) 임베딩·검색 — 남음**

- **SQLite → PostgreSQL 이전** (Docker로 로컬 기동). 왜 옮기는지를 먼저 문서에 쓰고 옮긴다
- 문서 청킹 및 임베딩 → pgvector 저장, 근사 최근접 인덱스 구성
- **하이브리드 검색** — 키워드 검색과 벡터 검색 결과 결합 및 재순위

**신규 기술**: SEC EDGAR API, PostgreSQL, Docker(로컬 DB 한정), pgvector, 임베딩 모델
**CS 학습 포인트**: rate limiting과 지수 백오프, 크롤링 시 이용약관/robots.txt, 트랜잭션과 격리 수준,
커넥션 풀, B-tree 인덱스와 HNSW 같은 근사 최근접 인덱스의 구조적 차이, 검색 랭킹과 하이브리드 결합 방식,
청킹 전략이 검색 품질에 미치는 영향

---

### M4 — 리포트가 나온다 (AI Agent + Report/API)

> **완료 조건**: 하루치 리포트 한 편이 자동 생성되고, **리포트의 모든 주장에 데이터 또는 문서 출처가 붙는다**.
> 출처 없이 단정하는 문장이 있으면 실패로 본다.

- Agent 루프 설계 — 도구 호출로 M2의 정량 결과와 M3의 검색 결과를 가져오게 한다
- **근거 강제 설계** — 모델이 아는 것으로 답하지 못하게 하고, 회수된 자료만 인용하게 한다
- 리포트 구조 정의 및 템플릿 렌더링
- 조회용 HTTP API — 날짜별 리포트, 섹터·종목 스코어 조회
- 리포트 품질 점검 방법 마련 (출처 누락 검출, 수치 일치 확인)

**신규 기술**: LLM API, Agent 오케스트레이션(프레임워크 또는 직접 구현), 웹 프레임워크, 템플릿 엔진
**CS 학습 포인트**: tool calling과 도구 스키마 설계, Agent 상태 관리, 컨텍스트 길이 관리와 비용,
REST API 설계와 상태 코드, 동기·비동기 처리, 외부 API 실패 시의 타임아웃·재시도

> 사용할 모델과 비용 구조는 이 단계의 학습 로그를 작성하는 시점에 확정한다.
> 기억에 의존해 적지 않고, 그 시점의 공식 문서를 확인해 기록한다.

---

### M5 — 사람 손 없이 돈다 (Deployment + CI/CD + Monitoring)

> **완료 조건**: **내 PC를 꺼도** 매일 리포트가 생성된다. 어딘가 실패하면 알림이 오고,
> 로그만 보고 어느 단계에서 무엇이 깨졌는지 알 수 있다.

- 전체 스택 컨테이너화 및 오케스트레이션 (앱 + DB + 스케줄러)
- 클라우드 이전 (D-3) — 무료 티어 VM 또는 저가 VPS
- CI — 린트·타입 체크·테스트 자동 실행
- CD — 이미지 빌드 및 배포 자동화
- 구조화 로깅 (단계·종목·소요시간을 필드로)
- 알림 — 수집 실패, 리포트 생성 실패, **데이터 신선도 이상**(오늘 데이터가 안 들어옴)
- 시크릿 관리 — API 키를 이미지·저장소에 넣지 않기

**신규 기술**: Docker/Compose, 클라우드 VM, GitHub Actions, 구조화 로깅, 알림 채널
**CS 학습 포인트**: 컨테이너 격리와 이미지 레이어, 볼륨과 데이터 영속성, 환경변수·시크릿 주입,
빌드/테스트/배포 파이프라인과 트리거 조건, 관측 가능성(로그·메트릭·알람), 알람 임계치 설계와 알람 피로

---

### 5.6 진행 순서에 대한 메모

- **M1 → M2 → M3 → M4 → M5 순서를 지킨다.** 특히 M4(Agent)를 앞당기고 싶은 유혹이 크지만,
  M2·M3가 없으면 Agent는 결국 "LLM에게 물어보기"가 되고, 이는 기획서 1장이 명시적으로 배제한 것이다
- 각 마일스톤 안에서는 **완료 조건을 먼저 만족시키고 다듬는다**. 폭을 넓히는 것(종목 수, 팩터 수,
  뉴스 소스 수)은 항상 나중이다
- 한 마일스톤이 2주 이상 늘어지면 완료 조건을 낮춰 잘라내고 다음으로 넘어간다.
  전 단계가 연결되어 돌아가는 경험이 한 단계의 완성도보다 우선한다

### 5.7 학습 로그 작성 계획

아래는 현재 예상되는 목록이다. 기술을 확정할 때마다 파일명을 채우고
`docs/tech-notes/README.md` 의 표에 등록한다.

| 마일스톤 | 예정 노트 | 비고 |
|---|---|---|
| M0 | `01-foundation-*` (의존성/설정 관리) | 기술 확정 후 파일명 결정 |
| M1 | `02-data-collection-yfinance`, `03-storage-sqlite` | |
| M2 | `04-processing-batch-pipeline`, `05-quant-factor-scoring` | 새로 배우는 것이 적으면 통합 가능 |
| M3 | `06-data-collection-sec-edgar`, `07-storage-postgresql`, `08-retrieval-embedding-pgvector`, `09-retrieval-hybrid-search` | `07`은 **이전 사유**를 핵심으로 |
| M4 | `10-agent-llm-api`, `11-agent-orchestration`, `12-api-fastapi` | |
| M5 | `13-deployment-docker`, `14-cicd-github-actions`, `15-monitoring-logging-alerting` | |

순번은 실제 도입 순서에 맞춰 조정한다. 표의 번호는 예상치일 뿐이다.

---

## 6. 이 프로젝트를 "끝냈다"고 말할 수 있는 조건

1. 매일 자동으로 리포트가 생성되고, **내 개인 장비와 무관하게** 동작한다
2. 리포트의 모든 주장에 **데이터 또는 원문 출처**가 붙어 있다
3. 파이프라인의 각 단계가 실패했을 때 **알림이 오고, 로그로 원인을 특정**할 수 있다
4. `docs/tech-notes/` 에 도입한 기술 수만큼의 학습 로그가 있고, 각 문서의
   **"한계점"과 "선택 이유" 절이 실제 경험으로 채워져** 있다
5. 저장소를 처음 보는 사람이 README만 읽고 **로컬에서 전체 파이프라인을 재현**할 수 있다

4번이 나머지보다 중요하다. 1~3번은 시스템의 완성도이고, 4번이 이 프로젝트의 실제 산출물이다.

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-27 | 4~6장 추가 — 프로젝트 결정(D-1~D-3), 마일스톤 M0~M5, 완료 조건 정의 |
| 2026-08-27 | M0 완료 — uv / pydantic-settings / ruff / mypy / pytest 도입, 학습 로그 2편 |
| 2026-08-27 | 5.0절 유니버스 확정 — `config/universe.toml` 초안 (11섹터 × 4종목 = 44종목) |
| 2026-08-27 | M1 완료 — yfinance 수집 + SQLite 적재, 학습 로그 2편. 스케줄링은 M5 로 이동 |
| 2026-08-27 | M2 완료 — 4팩터 스코어링 + 섹터 랭킹, 룩어헤드 차단 실증. 학습 로그 1편 |
| 2026-08-27 | M3(a) 완료 — SEC 공시 411건 + 뉴스 560건 수집. 뉴스 소스는 yfinance 내장으로 확정 |
